openapi: 3.0.3
info:
  title: Multi-Tenant Management API
  version: 1.0.0
  description: Multi-tenant management for SyriusOrbit platform
paths:
  /tenants:
    get:
      summary: List tenants
      operationId: listTenants
      responses:
        '200':
          description: List of tenants
