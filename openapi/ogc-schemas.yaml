openapi: 3.0.3
info:
  title: OGC API - Schemas
  version: 1.0.0
  description: OGC API Schemas for data schema discovery in SyriusOrbit
paths:
  /schemas:
    get:
      summary: Get available schemas
      operationId: getSchemas
      responses:
        '200':
          description: List of schemas
